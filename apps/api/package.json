{
  "name": "@axos/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run build --workspace=@axos/database",
    "dev": "tsx watch src/server.ts",
    "prebuild": "npm run build --workspace=@axos/database",
    "build": "tsc",
    "pretypecheck": "npm run build --workspace=@axos/database",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@axos/database": "*",
    "@fastify/cors": "^11.0.0",
    "dotenv": "^17.0.0",
    "fastify": "^5.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
